# AVANI LOAN SERVICES - Master Ecosystem Implementation Plan

Based on the Master System Prompt, I have audited the workspace and mapped out the 12 phases for full execution.

## User Review Required

> [!WARNING]
> This is a massive operation spanning all your repositories (`3-AVANI AI CRM`, `4-AVANI LOAN AGENTS`) and 3rd party APIs (OmniDM, AiSensy, HubSpot, Zapier, Meta).
> Before I proceed with **Execution Mode**, please review this plan and click **Proceed**. Once approved, I will work continuously through all phases in auto mode.

## Phase 1: Complete Audit & Auto-Fixes
- Check `3-AVANI AI CRM` and `4-AVANI LOAN AGENTS` for security vulnerabilities (hardcoded API keys, unencrypted configs).
- Move all API keys (OmniDM, AiSensy, HubSpot, Zapier) to strict `.env` variables.
- Fix the existing `/broadcasts` 500 error on Vercel by tracking down the faulty `redirect()` or caching issue in `frontend-liart-gamma-68`.
- Fix the OmniDM `dispatch` API error in the Campaigns dashboard by injecting the correct `agent_id` (e.g., `#229425` or `#228450`) into the API payload.

## Phase 2: AiSensy (WhatsApp) Sync
- **Update Business Details**: Inject the "Avani Loan Services" profile (address, email, target audience) directly into the AiSensy configuration using their API.
- **Sync Contacts & Broadcasts**: Connect the CRM's backend to AiSensy so that when a Lead is created, it triggers an AiSensy template message.

## Phase 3: OmniDM Sync
- **Update Business Details**: Inject the business profile into the OmniDM assistant prompts automatically.
- **AI Call Scheduling**: Connect `avani-ai-crm` to OmniDM so that a Lead status change triggers a call using one of your 3 agents (`229425`, `228450`, `207978`).
- Map OmniDM callbacks back to the CRM to store recording URLs and transcriptions.

## Phase 4: AI Agent (`4-AVANI LOAN AGENTS`)
- Update the AI Agent repo to query the CRM database before answering questions (so it knows the customer's history).
- Expose an endpoint that allows the AI Agent to trigger a WhatsApp message via AiSensy.

## Phase 5: Broadcast System
- Fix the Vercel issue on the `/broadcasts` page.
- Ensure the CSV upload mechanism maps phone numbers correctly and validates them before passing them to OmniDM and AiSensy.

## Phase 6: Meta (Facebook/Instagram) Integration
- Set up a webhook endpoint `/api/meta-webhook` in the CRM.
- Map incoming Meta Lead Ads JSON payloads to the CRM database and immediately trigger the "Phase 8" automation flow.

## Phase 7: Voice AI Abstraction
- Ensure `omnidim.ts` acts as an interface. If the provider changes in the future, only this file will need editing, not the core business logic.

## Phase 8: CRM Automated Workflows
- Build the master pipeline: `New Lead -> AiSensy Welcome -> OmniDM Call -> Google AI Studio Parsing -> Assign to Sales -> HubSpot Sync -> Google Sheet Sync`.

## Phase 9 & 10: Security & Performance
- Rate limit the `/api/omnidim-webhook` and `/api/meta-webhook` routes.
- Implement server-side caching for the dashboard routes to improve Vercel performance.

## Phase 11 & 12: Testing & Deployment
- E2E tests using simulated webhooks.
- Final auto-deployment to Vercel and pushing all code to GitHub.

---

### Open Questions
> [!IMPORTANT]
> 1. For OmniDM, which specific Agent ID should I use for general campaigns? `229425` (Marathi) or `228450` (English/Hindi)?
> 2. Do you have the Meta App ID / Webhook Secret ready to be added to the environment variables, or should I just build the receiver structure for now?
